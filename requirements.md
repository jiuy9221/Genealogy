# Genealogy Web App - Requirements (v1)
离线族谱应用（纯前端，无后端）

## 核心需求
1. **人员管理**
   - 添加人员（姓名、性别、出生、死亡、备注）
   - 编辑人员
   - 删除人员
   - 列表显示所有人员

2. **关系管理**
   - 添加父母
   - 添加子女
   - 添加配偶
   - 自动构建家庭结构

3. **族谱树展示**
   - 使用 D3.js 或 Canvas 绘制树结构
   - 支持无限代
   - 父母在上，子女在下
   - 配偶横向连接

4. **数据存储（双格式）**
   - family.json（结构化数据）
   - family.md（Markdown 轻量文本）
   - 数据双向转换（md <-> json）
   - 自动本地缓存（localStorage）

5. **数据操作功能**
   - 导入 JSON
   - 导入 Markdown
   - 导出 JSON
   - 导出 Markdown
   - 自动保存按钮

6. **UI 需求**
   - 左侧：人员列表
   - 中间：族谱树显示
   - 右侧：人员信息编辑
   - 顶部按钮：导入 / 导出 / 新增人员

7. **离线运行要求**
   - index.html 在没有网络情况下可直接使用
   - 所有 JS、CSS、本地图标必须本地引用

## 已实现增强功能（截至 2026-05-03）
- [x] 族谱树 SVG 渲染（层级布局、渔骨连线）
- [x] 暗色/亮色主题切换（Dark Mode）
- [x] 节点拖拽重排（localStorage 持久化）
- [x] 搜索过滤人员列表（实时关键字 + 高亮）
- [x] 导入合并/覆盖对话框
- [x] Toast 通知系统
- [x] 统计视图（成员数、性别比、代际数、平均寿命）
- [x] 导出 PNG（2× 高清 Canvas）
- [x] 分享链接（URL hash base64 编码）
- [x] 打印优化（@media print 横向输出）
- [x] 头像上传（base64 存储，300KB 限制）
- [x] 时间轴视图（按出生年展示各代成员）
- [x] 节点选中自动居中
- [x] **多语言支持**（简体中文 / 繁體中文 / English，localStorage 持久化）
- [x] **祖先路径显示**（右侧面板面包屑，可点击跳转）
- [x] **触摸双指缩放**（pinch-to-zoom，防止浏览器原生缩放冲突）

## 后续增强（future）
- 多族谱文件管理
- 搜索支持拼音首字母模糊匹配
- 人员头像从 URL 加载（减小 localStorage 占用）
- 导出 PDF（jsPDF）
- 合并排除重复家族